# Copy into .env.local (never commit real secrets)

# App
DATABASE_URL=
JWT_SECRET=
APP_URL=http://localhost:3000

# Brevo transactional email (tenant admin invitations)
BREVO_API_KEY=
BREVO_SENDER_EMAIL=
BREVO_SENDER_NAME=BuildX
